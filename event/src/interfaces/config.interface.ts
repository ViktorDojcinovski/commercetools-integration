export interface Config {
  clientId: string;
  clientSecret: string;
  projectKey: string;
  scope: string;
  region: string;
  port?: string;
  vsPassword: string;
  vsUsername: string;
  vsApi_v4: string;
  edgeApi_v4: string;
}
